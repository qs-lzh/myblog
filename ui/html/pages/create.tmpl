{{define "content"}}

<form action="/create" method="post" class="space-y-4">

  <!-- Title -->
  <div>
  {{with .Form.FieldErrors.title}}
  <br>
    <div class="flash bg-red-100 border border-red-400 text-red-900 px-4 py-3 rounded relative" role="alert">
      <span class="block sm:inline">{{.}}</span>
    </div>
  <br>
  {{end}}
    <label for="title" class="block text-sm font-medium text-gray-700">Title</label>
    <textarea id="title" name="title" rows="1"
      class="mt-1 w-full rounded-lg border-gray-300 shadow-sm focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50">
      {{.Form.Title}}
    </textarea>
  </div>

  <!-- Content -->
  <div>
  {{with .Form.FieldErrors.content}}
  <br>
    <div class="flash bg-red-100 border border-red-400 text-red-900 px-4 py-3 rounded relative" role="alert">
      <span class="block sm:inline">{{.}}</span>
    </div>
  <br>
  {{end}}
    <label for="content" class="block text-sm font-medium text-gray-700">Content</label>
    <textarea id="content" name="content" rows="10"
      class="mt-1 w-full rounded-lg border-gray-300 shadow-sm focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50">
      {{.Form.Content}}
    </textarea>
  </div>

  <div>
    <label for="date">Choose the due date</label>
    <input type="date" id="date" name="date">
  </div>

   <!-- 提交按钮 -->
  <button type="submit"
    class="w-full bg-blue-600 text-white font-semibold py-2 px-4 rounded-lg shadow hover:bg-blue-700 transition duration-200">
    Create
  </button>
</form>


{{end}}
